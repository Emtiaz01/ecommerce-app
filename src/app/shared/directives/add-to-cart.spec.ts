import { AddToCartDirective } from './add-to-cart';

describe('AddToCart', () => {
  it('should create an instance', () => {
    const directive = new AddToCartDirective();
    expect(directive).toBeTruthy();
  });
});
