import { AddToWishlistDirective } from './add-to-wishlist';

describe('AddToWishlist', () => {
  it('should create an instance', () => {
    const directive = new AddToWishlistDirective();
    expect(directive).toBeTruthy();
  });
});
