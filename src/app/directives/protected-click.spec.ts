import { ProtectedClickDirective } from './protected-click';

describe('ProtectedClick', () => {
  it('should create an instance', () => {
    const directive = new ProtectedClickDirective();
    expect(directive).toBeTruthy();
  });
});
